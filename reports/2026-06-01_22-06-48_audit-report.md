# Audit Report — DigitalerUnterlagenOrdner

**Date:** 2026-06-01 22:06:48
**Scope:** Whole project (legacy Tk app + React/Vite UI + data-driven `core/` + pywebview `host.py`)
**Reviewer:** Opus agent (fresh-eyes review) + pre-flight checks

---

## Pre-flight checks

| Check | Result |
|---|---|
| `npm run build` (webui) | ✅ succeeds |
| `pytest` | ✅ **271 passed, 9 skipped** |
| `npm run lint` (webui) | ⚠️ **4 errors** (see findings) |
| `FEATURES_REQUIRED.md` | ✅ present at root |
| Test files | 49 `test_*.py` |

**Overall:** Architecture is strong — `core/` is pure, immutable, UI-free and is the single source of truth for the React path. Security basics are well handled (executable-signature rejection, zip/tar bomb guards, Office macro disabling, remote-link blocking for HTML mail; no pickle/eval/shell/yaml). **0 Critical findings.**

---

## Prioritized findings

| Category | Severity | Finding | Location | Suggestion |
|---|---|---|---|---|
| Security | High | Parked named-pipe service creates the pipe with `None` security + a guessable per-user name; any local process could connect and drive `dispatch/open/save`. | `core/pipe.py:75` `create_server_instance`, `:24` `default_pipe_name` | Set a DACL to the user's SID, or — since the service is never started by `host.py` — drop `server.py`/`pipe.py` from the build to prevent accidental activation. |
| Correctness | High | `_win()` falls back to `webview.windows[0]` when the uid isn't found, so a native dialog (open/save/export/import) can open against the **wrong** window if its window was closed mid-session. | `host.py:_win()` (~119-123); callers `save_file/open_file/export_dialog/import_dialog` | Return an explicit error instead of silently using window 0; never run save/export against an unrelated document. |
| Error handling | High | `_prewarm` wraps everything in `except Exception: pass` — if the heavy import path breaks (missing DLL, version drift) it's invisible and only surfaces later as a slow/failed first action. | `host.py:_prewarm` (~58) | `logger.exception(...)` inside the warm path; keep non-fatal but diagnosable. |
| Correctness | Medium | Multi-file drop import passes `index+i` per file, but one imported file can expand to multiple nodes (e.g. a `.belegtool`/structured file → several children), so positions can collide/interleave at a fixed drop index. | `webui/src/App.jsx` `onDropFiles` (~185-207); `core/api.py` `import_paths`/`InsertNodes` | Import the dropped batch in one `import_paths` call (collect nodes, insert contiguously) instead of per-file index arithmetic. |
| Error handling | Medium | Structured import failures (zip/tar/email) are swallowed to a `logger.warning` then fall through to plain-PDF import, so a corrupt `.zip` yields an opaque "not a PDF" error. | `pdf_storage.py:109-111` | Surface the structured-import error via the existing React `warning` channel. |
| Error handling | Medium | `PDFNode` preview build swallows all exceptions to an empty preview with no log — broken `current_data` looks identical to an empty folder. | `pdf_node.py:853-854` (and ~932) | Add `logger.warning` so silent preview failures are traceable. |
| Code quality (lint) | Medium | `setState` inside effects (`App.jsx:132/150`, `PreviewControls.jsx:23`) — benign, but the listener effects (drop/keydown/wheel) run on **every** render with no dep array, rebinding listeners each time. | `App.jsx` (~105,141,147,152,229,340); `PreviewControls.jsx:25` | Give the listener effects proper dep arrays (or `useEffectEvent`); suppress the intended set-state-in-effect lines with a comment. |
| Code quality | Low | Dead branch: `(k === 'z' && e.shiftKey)` is unreachable — the previous line already handles Ctrl+Shift+Z. | `webui/src/App.jsx:333-334` | Simplify to `mod && k === 'y'`. |
| Code quality (lint) | Low | `'parentName' is defined but never used`. | `webui/src/Tree.jsx:47` | Remove `parentName` from the `TreeNode` destructure. |
| Correctness | Low | A selected leaf with `pdf_length 0`/no data is silently dropped from the export TOC with no notice. | `core/api.py:182-198`, `toc_export.py:_build_toc_items:75-77` | Optionally warn when a selected node contributes 0 pages. |
| Performance | Low | `export`/`save` build a full mutable `PDFStorage` tree (deep byte copy) on the JS-bridge thread, blocking for large docs. | `core/api.py` `export` (~171-205), `core/bridge.py:document_to_storage` | Acceptable now; offload to the `tasks` worker port if docs grow. |
| Performance | Low | `parent_of` is O(n); `MoveMany`/`_top_level_ids` call it in nested loops → O(n²) on large trees. | `core/model.py:154-158`, `core/commands.py:297-312` | Fine for receipt-sized trees; build a child→parent map if needed. |
| Test coverage | Medium | `host.py` (per-window HostApi, `_win`, close guard, `new_window`) and the React UI have **no** automated tests. | `host.py`, `webui/src/*` | Unit-test `HostApi` delegation + `_win()` with a fake `webview.windows`; consider a Vitest smoke test for `core.js`. |
| Docs drift | Low | `CLAUDE.md` says "31 test files"; there are 49. Still references `react-migration` branch as if unmerged. | `CLAUDE.md:~183` | Update count + migration wording. |
| Docs | Low (verified) | `docs/data-model.html` + `DATA_CONTRACT.md` include `compression_method`, `GroupIntoFolder`, `InsertNodes` — in sync. | `docs/data-model.html:163-164` | None — "regenerate on model change" honored. |

---

## FEATURES_REQUIRED.md verification

All six critical paths are implemented in the React/core path; **no silent failures**:

1. **Import → tree** — `_import_path` routes PDF/belegtool/zip/tar/eml/msg via `PDFStorage`, images/Office via `UniversalImporter`; zip/tar bomb limits enforced. ✅
2. **Tree editing** — Split/Merge/AddFolder/Rename/Delete/Move/MoveMany/GroupIntoFolder all have reducer handlers + tests. ✅
3. **Preview & compression** — lazy `render_compressed` (no mutation), method dropdown, Commit/Reset/`no_compression`, DPI-conflict invariant. ✅
4. **Status system** — `SetStatus` validates against `STATUSES`. ✅
5. **Export** — `export_pdf_with_toc` with clickable links + bookmarks + >100-page split (lossy for structured text PDFs — documented). ✅
6. **Persistence** — `save_belegtool`/`load_belegtool` round-trip; `is_compressed`/`compression_method` restored on load. ✅

---

## Summary

- **Counts:** Critical 0 · High 3 · Medium 5 · Low 6.
- **Top 3 high fixes:**
  1. `_win()` wrong-window fallback for native dialogs — fail explicitly (`host.py`).
  2. DACL-harden or remove the parked named-pipe service (`core/pipe.py`).
  3. Log `_prewarm` failures instead of silent `pass` (`host.py`).
- **Top 2 architectural improvements:**
  1. Add a test seam for the host glue (`HostApi`/`_win`/close-guard) — the one untested layer between two well-tested sides.
  2. Batch the React drop import into a single `import_paths` call to remove per-file index arithmetic and its edge cases.
- **Quick wins:** remove dead Ctrl+Shift+Z branch; drop unused `parentName`; give drop/keydown/wheel effects dep arrays; fix "31 test files" count in CLAUDE.md; add `logger.warning` to the two silent-swallow sites.

**No** command injection, unsafe deserialization (JSON-only), secrets, or path traversal found in the active import/export/temp-file pipeline; temp files use `mkdtemp` + `finally` cleanup, and `os.path.basename` strips path components from dropped names.
