# Audit Report — DigitalerUnterlagenOrdner (BelegTool)

**Date:** 2026-06-01 22:44:47
**Scope:** Whole project — legacy Tk app + React/Vite UI (`webui/`) + data-driven `core/` + pywebview `host.py`
**Reviewer:** Opus inline audit + pre-flight checks
**Baseline:** Builds on the 2026-06-01 22:06:48 audit; verifies that report's fixes landed and looks for new issues.

---

## Pre-flight checks

| Check | Result |
|---|---|
| `pytest` | ✅ **278 passed, 9 skipped** (120 s) |
| Coverage | 72 % total (low spots: `universal_importer` 29 %, `view_tree` 29 %, `view_preview` 55 %, `toc_export` 60 %) |
| `npm run build` (webui) | ✅ succeeds (208 kB JS / 66 kB gzip, 327 ms) |
| `npm run lint` (webui) | ✅ **0 errors** |
| `requirements.txt` | ✅ fully pinned (`==`) |
| `webui` deps | ✅ React 19.2.6 / Vite 8.0.12 — match workspace baseline |
| `FEATURES_REQUIRED.md` | ✅ present; all 6 paths implemented |

**Overall:** Excellent shape. The `core/` layer is pure, immutable, UI-free and the single source of truth for the React path; security basics are strong (named-pipe DACL, zip/tar bomb guards, Office macro disable via `AutomationSecurity=3`, remote-link blocking for HTML mail, no eval/pickle/shell). **All High/Medium findings from the previous audit have been fixed** (verified below). **0 Critical, 0 High.**

### Previous-audit fixes verified

| Prior finding | Status |
|---|---|
| Named-pipe `None` security | ✅ Fixed — `_owner_only_sa()` DACL grants only current-user SID + LocalSystem (`core/pipe.py:24-43,105`) |
| `_win()` fell back to `windows[0]` | ✅ Fixed — now returns `None`; callers return an explicit error (`host.py:122-128`) |
| `_prewarm` swallowed import failures | ✅ Fixed — `logger.exception("prewarm failed")` (`host.py:59-62`) |
| Multi-drop fixed-index collision | ✅ Fixed — multiple files append (`useIndex = list.length === 1 ? index : null`, `App.jsx:194`) |
| Structured-import error opaque "not a PDF" | ✅ Fixed — surfaced via the `warning` channel (commit 68ffd9e) |
| Lint: unused `parentName` | ✅ Fixed — now consumed by the drop-ghost (`Tree.jsx:125`) |

---

## Prioritized findings

| Category | Severity | Finding | Location | Suggestion |
|---|---|---|---|---|
| Consistency | Medium | **Two product names ship in one codebase.** The active React/pywebview UI titles itself **"DigitalerBelegeOrdner"** while the legacy Tk app + `APP_NAME`/repo/README say **"DigitalerUnterlagenOrdner"**. The window title and header brand differ from everything else. | `host.py:101` (window title), `webui/src/App.jsx:356` (`<h1>`/`title`) vs `version_info.py:2` `APP_NAME`, dir/repo, `README.md` | Pick one brand. If "DigitalerBelegeOrdner" is intended, update `APP_NAME` + docs; otherwise use `get_full_title()`/`APP_NAME` in `host.py` and `App.jsx` so the name is single-sourced. |
| Test coverage | Medium | `host.py` (HostApi delegation, `_win()`, close guard, `new_window`) and the **entire React UI** have no automated tests. `host.py` is pure Python and unit-testable with a fake `webview.windows`. | `host.py`, `webui/src/*` | Add `tests/test_host_api.py` (fake `webview.windows` → assert `_win()` returns `None` when uid is gone, delegation forwards to `CoreApi`). Add a Vitest smoke test for `core.js`/reducers-of-the-UI (already on your open list). |
| Docs drift | Low | `CLAUDE.md` claims "49 test files (271 passing)"; actual is **53 files, 278 passing**. | `CLAUDE.md:183` | Update the count, or drop the exact number and say "see `tests/`". |
| Correctness | Low | A selected leaf with `pdf_length 0` / no bytes is silently omitted from the export TOC with no notice (already on your open list — cosmetic, rarer since `pdf_length` was fixed). | `core/api.py:182-200`, `toc_export.py` `_build_toc_items` | Optionally collect skipped-node names and return them in the export `warning` channel. |
| Performance | Low | The drop/keydown/wheel listener effects intentionally have **no dependency array**, so they rebind `window` listeners on every render. Documented as intentional (to close over current state). | `webui/src/App.jsx:95-105,213-231,324-342` | Benign at this scale. If churn matters, switch to refs + a single mount effect, or `useEffectEvent`. |
| Performance | Low | `export`/`save` build a full mutable `PDFStorage` deep byte-copy on the pywebview bridge thread (off the GUI thread, so no UI freeze — already on your open list). | `core/api.py:173-207`, `core/bridge.py:document_to_storage` | Acceptable now; offload to the `tasks` worker port only if documents grow very large. |

---

## FEATURES_REQUIRED.md verification

All six critical paths are implemented in the active React/core path — **no silent failures**:

1. **Import → tree** — `_import_path` routes PDF/belegtool/zip/tar/eml/msg via `PDFStorage`, images/Office via `UniversalImporter`; zip/tar member-count (500) + 500 MB uncompressed bomb guards enforced. ✅
2. **Tree editing** — Split / Merge / AddFolder / Rename / Delete / Move / MoveMany / GroupIntoFolder all have reducer handlers + tests. ✅
3. **Preview & compression** — transient `render_compressed` (no mutation), method dropdown, Commit/Reset/`no_compression`, DPI-conflict invariant (`_merge_compression_state`). ✅
4. **Status system** — `SetStatus` validates against `STATUSES`. ✅
5. **Export** — `export_pdf_with_toc` with clickable links, bookmarks, >100-page auto-split. ✅
6. **Persistence** — `save_belegtool` / `load_belegtool` round-trip the node schema. ✅

---

## Summary

- **Counts:** Critical 0 · High 0 · Medium 2 · Low 4.
- **Health:** Strong. The previous audit's 3 High + 5 Medium findings are all fixed and verified in code. Tests green (278), lint clean, build clean, deps pinned.

### Top fixes worth doing
1. **Unify the product name** (Medium) — decide between "DigitalerBelegeOrdner" and "DigitalerUnterlagenOrdner" and single-source it; right now the window header contradicts the repo/README.
2. **Add `host.py` unit tests** (Medium) — the per-window `HostApi`/`_win()`/close-guard logic is the one untested seam in an otherwise well-tested core; trivially testable with a fake `webview.windows`.

### Architectural notes (no action required)
- The data-driven `core/` (immutable `Node`/`Document`, pure reducer with a handler registry, structural sharing) is a model example of the workspace's logic/UI-separation principle — keep new behavior there.
- The headless `services/render` + `progress`/`tasks` ports cleanly decouple the GUI; the React migration is effectively complete on the active path.

### Quick wins
- Fix the `CLAUDE.md` test count (53 files / 278 passing).
- Add the export 0-page `warning` (one list + one string join).

### Pre-flight results
- **Build:** ✅ webui builds. **Lint:** ✅ 0 errors. **Tests:** ✅ 278 passed / 9 skipped. **Deps:** ✅ pinned, on-baseline.
