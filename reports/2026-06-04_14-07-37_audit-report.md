# Audit Report — DigitalerUnterlagenOrdner (BelegTool)

**Date:** 2026-06-04 14:07:37
**Scope:** Whole project — React/Vite UI (`webui/`), data-driven `core/`, pywebview `host.py`, import/export, named-pipe CLI.
**Focus (requested):** code design & readability, and security.
**Reviewer:** Opus inline audit + pre-flight checks
**Baseline:** First audit after the v3.7.x cleanup — PDFNode reduced to a pure `.belegtool` I/O carrier, dead `progress.py`/`config.txt` removed, `sanitize_pdf` re-wired into imports.

---

## Pre-flight checks

| Check | Result |
|---|---|
| `pytest` | ✅ **308 passed** (~40 s) |
| `vitest` (webui) | ✅ **76 passed** (9 files) |
| `npm run build` (webui) | ✅ succeeds (229 kB JS / 73 kB gzip, ~230 ms) |
| `npm run lint` (webui) | ⚠️ **7 errors, 2 warnings** — regression from the prior audit's 0 errors |
| `requirements.txt` | ✅ fully pinned (12× `==`) |
| `webui` deps | ✅ React 19.2.6 / Vite 8.0.12 — on workspace baseline |
| `FEATURES_REQUIRED.md` | ✅ present; all 6 paths implemented |
| Dangerous primitives | ✅ none — no `eval`/`exec`/`pickle`/`os.system`/`subprocess`/`shell=True`/`yaml.load` |

**Overall:** Strong. The `core/` layer remains a model of the workspace's logic/UI-separation principle (immutable `Node`/`Document`, pure reducer with a handler registry, structural sharing, engine/services injected and headless-testable). Security basics are solid: per-user named-pipe DACL, zip/tar bomb guards, EXE/script-signature refusal, Office-macro force-disable, rasterised previews (no HTML/JS from document content), JSX-escaped names. Two issues warrant attention: a **local-file-inclusion gap in HTML/email import** and a **lint regression (0 → 7 errors)**. **0 Critical, 0 High.**

---

## Prioritized findings

| Category | Severity | Finding | Location | Suggestion |
|---|---|---|---|---|
| Security | **Medium** | **HTML/email import allows local-file inclusion.** `_block_remote_link` blocks only `http://`/`https://`/`//`; it **returns (allows)** every other URI — including `file:///C:\…`, absolute and relative local paths. A malicious `.html`/`.eml`/`.msg` with `<img src="file:///C:/Users/<u>/…/secret.pdf">` makes xhtml2pdf load that local file and embed it into the converted PDF → local data disclosure if the user then shares the export. | `universal_importer.py:285-294` (`_block_remote_link`), used at `:305` | Invert to an **allowlist**: permit only `cid:` and `data:` URIs (and already-embedded content); return `None` for everything else — remote **and** local/`file:`/relative. Add a regression test (`<img src="file:///…">` is dropped). |
| Code quality | **Medium** | **webui lint regressed 0 → 7 errors** (newer react-hooks ruleset). setState-synchronously-in-effect (`App.jsx:155`, `Preview.jsx:51,74`), a `useRef` not named `*Ref` (`Tree.jsx:124` `renameTimer`), files exporting non-components breaking fast-refresh (`ContextMenu.jsx:9`, `LanguageProvider.jsx:34`), `process` undefined in a test (`i18n/i18n.test.js:53`), plus a **misplaced `eslint-disable`** (`App.jsx:156` reported unused while `155` is flagged). None crash; mostly hygiene/correctness-leaning. | `webui/src/{App,Preview,Tree,ContextMenu}.jsx`, `webui/src/i18n/{LanguageProvider,i18n.test}.js` | Move shared constants out of component files (own module); add the node/test env for `*.test.js`; rename `renameTimer`→`renameTimerRef`; scope the two intentional reset-in-effect suppressions to the exact line. Re-establish **0-error lint** as a gate. |
| Readability | Low | **Dead commented code** — the SVG/cairosvg conversion path is fully commented out (imports + branch + method). Adds noise to a large module. | `universal_importer.py:16,36,201-202,251-256` | Delete; restore from git if SVG support is revived. |
| Docs | Low | **CLAUDE.md version stale** — says `3.7.0` while `version_info.py` is `3.7.1` and tag `v3.7.1` is pushed. | `CLAUDE.md:9,50,289,371` | Bump the four references to 3.7.1. (Fixed in this audit pass.) |

---

## FEATURES_REQUIRED.md verification

All six critical paths implemented and green (308 pytest + a carrier round-trip smoke test run this session) — no silent failures:

1. **Import → tree** — PDF/belegtool/zip/tar/eml/msg via `PDFStorage`; images/Office/HTML via `UniversalImporter`; bomb guards + EXE-signature refusal enforced. `sanitize_pdf` now repairs malformed plain PDFs on import (and never the `.belegtool` path). ✅
2. **Tree editing** — Split/Merge/AddFolder/Rename/Delete/Move/MoveMany/GroupIntoFolder reducer handlers + tests; UI selection resolver for mixed parent/child sets. ✅
3. **Preview & compression** — transient `render_compressed` (no mutation), method dropdown, Commit/Reset/`no_compression`, DPI-conflict invariant; variants persist in the `.belegtool`. ✅
4. **Status system** — `SetStatus` validated against `STATUSES`. ✅
5. **Export** — `export_pdf_with_toc` (clickable TOC, bookmarks, >100-page auto-split); empty leaves surfaced via the `warning` channel. ✅
6. **Persistence** — `save` in place (binds session→path), node ids round-trip in `/JSONStructure`, committed nodes reload coherently. ✅

---

## Summary

- **Counts:** Critical 0 · High 0 · Medium 2 · Low 2.
- **Health:** Strong. Tests green (308 py / 76 js), build clean, deps pinned + on-baseline. The v3.7.x cleanup removed the last large dead seam (PDFNode machinery) without regression.

### Top fixes worth doing
1. **Close the HTML/email LFI** (Medium, Security) — invert `_block_remote_link` to a `cid:`/`data:` allowlist + regression test. Small and safe.
2. **Restore 0-error lint** (Medium, Quality) — triage the 7 errors; they're mostly mechanical (env/naming/file-split) with two real setState-in-effect spots to scope.

### Quick wins
- Delete the commented SVG/cairosvg code in `universal_importer.py`.
- Sync the CLAUDE.md version to 3.7.1.

### Architectural notes (no action required)
- Security model is sound for a single-user local desktop app: the pipe DACL grants only the current user + LocalSystem; the GUI exposes `CoreApi` only to the **bundled** React (not remote content); document names render via JSX (escaped); previews are PyMuPDF rasterisations, so no HTML/JS executes from imported PDFs.
- `core/` is the reference layer — keep new behaviour there (pure handlers + injected engine), as the PDFNode reduction reinforced.
