# BelegTool (DigitalerUnterlagenOrdner) — Quality Audit (follow-up)

**Date:** 2026-06-08 12:24:36
**Commit reviewed:** `06bf313` (master) — WIP toward a future build (no version bump)
**Reviewer:** Opus agent (read-only), cross-referenced with the prior report `2026-06-07_22-22-30_audit-report.md`
**Focus:** changes since the prior audit — committed-node split/rotate/merge fixes, command-error localization, en-US/en-GB split, Quenya/Sindarin, help content.

---

## Pre-flight check results

| Check | Result |
|---|---|
| `eslint .` (webui) | ✅ clean |
| `vitest run` (webui) | ✅ 144/144 (after fixing a stale HELP-count test, commit `06bf313`) |
| `pytest` (root) | ✅ 414 passed |
| `vite build` | ✅ success (~382 kB main / ~109 kB gzip; HelpModal lazy-chunked) |
| FEATURES_REQUIRED.md | ✅ present (7 critical paths) |

---

## Findings (prioritized; NEW vs carry-over marked)

| Category | Severity | Finding | Location | Suggestion |
|---|---|---|---|---|
| Correctness / data invariant | **High · NEW** | Merging two **committed** (source-less, irreversible) leaves with the **same DPI** takes the `preserves` branch and sets `merged_original = engine.merge([… n.current_data …])` → the merged node gets `original_data` = the already-compressed bytes, **resurrecting a fake "source"**: `has_source` flips True, `Compress`'s `if not node.original_data` guard passes (→ double-compression of compressed bytes), and Reset is no longer blocked — violating the drop-source/irreversible contract committed nodes must keep. Regression from the merge fix in `3f3bbb0`. | `core/commands.py:_merge` (`merged_original` assigned even when all inputs committed) vs `_split_part_committed` which correctly keeps `original_data=None` | When **all** inputs are committed (`all(n.original_data is None …)`), build the merged node committed: `original_data=None`, `current_data=engine.merge([… current_data …])`, `is_compressed=True`. Add a regression test asserting `original_data is None` / `has_source` False. |
| i18n correctness | **High · NEW** | The localized-errors work covered the 13 `CommandError` strings but **not the host-level `CoreApi` German error/warning strings**, which also flow to `setError → t(error)` and fall back to German for every non-German UI: file-in-use lock msg, `"nichts zu exportieren"`, `"nichts angezeigt"`, import `"nichts importiert"`, `"ungültige Daten: {e}"`, the "Ohne Seiten übersprungen…" / "Teilweise importiert…" warnings, and every `_friendly_import_error` message. None are keys in `en.js`. | `core/api.py:34-49,279,642,655,673,683,762,784`; surfaced at `webui/src/App.jsx:222,514` | Move the static ones into `en.js` (+ full-coverage langs) so `t()` localizes them, **or** return a stable error **code + params** and let the UI translate (also solves the dynamic per-file import errors, which need a parameterized template). |
| Test coverage / drift | **Medium · NEW** | The i18n coverage lock scans only 5 component files for `t('…')` literals; `Toolbar, TagEditor, TagViewBar, ExportDialog, HelpModal` are **not** scanned. All their current literals exist in `en.js` (checked) — nothing leaks today — but a new German literal in those five would ship untranslated unnoticed. | `webui/src/i18n/i18n.test.js` (`files` array) | Add the five components (or glob `src/**/*.jsx`). |
| Spec / integration | Medium · carry-over | Export **auto-split >100 pages** still not wired. Documented known gap (FEATURES §5 / CLAUDE.md / BETA_TESTING); user is actively designing it. Not re-flagged. | `formats/toc_export.py` `export_pdf` vs `export_pdf_split_with_toc` | Tracked — no action. |
| Security | Medium · carry-over (⚠ see note) | Prior report's ZIP-bomb "declared `file_size`" item. **NOTE: this was actually addressed in commit `3cef204`** (per-member capped read + `_ArchiveTooLarge`); the agent didn't re-verify and surfaced it from the old report. Treat as **resolved**, pending a glance. | `universal_importer/archives.py` | Verify `3cef204` cap is intact; otherwise no action. |
| i18n hard-count | Low | The `134` key-count lock + the "134 = 121 + 13" prose in CLAUDE.md must be bumped by hand together; adding the host-error keys touches three places. | `i18n.test.js`; `en.js`; CLAUDE.md | Bump lock + prose in the same commit as the High-2 fix. |
| Doc drift | Low · NEW | FEATURES_REQUIRED §7 and CLAUDE.md say **"18 languages"**; `index.js` now registers **22 selectable** (de + 21). | `FEATURES_REQUIRED.md:61`; CLAUDE.md i18n section vs `webui/src/i18n/index.js` | Update to 22. |
| i18n honesty | Low · verified OK | Quenya/Sindarin partials are honest (header comments, attested words + glosses, Sindarin multi-word avoided due to mutations). No action. | `webui/src/i18n/qya.js`, `sjn.js` | None. |
| Correctness | Low · verified OK | Committed-node Split/SplitInto/Rotate reviewed and correct; `_carries_compression` consulted only on the uncommitted branch (committed pikepdf node not mishandled); rotate-committed stays committed. Edge: committed-`no_compression` split drops the flag (likely fine, undocumented). | `core/commands.py` split/rotate handlers | Optional low-value test. |

---

## FEATURES_REQUIRED.md critical-path validation
§1 import→tree ✅ · §2 tree editing (incl. committed split/rotate now fixed) ✅ · §3 preview/compression ✅ · §4 status ✅ · §5 export ⚠ TOC/links/bookmarks OK, auto-split is the tracked known gap · §6 persistence ✅ (committed merge is the High-1 caveat) · §7 language/statusbar/windows ✅ (doc count stale: 22 not 18).

## Security
No `eval`/`exec`/`pickle`/`shell=True` introduced; "not pickle" claim holds (the grep hits are method names/comments). pywebview JS-bridge surface unchanged.

---

## Summary
- **Counts (new this pass):** Critical 0 · High 2 · Medium 1 (+2 carry-over, one likely already resolved) · Low 6.
- **Top 3 high-priority fixes:** (1) `_merge` all-committed same-DPI must keep `original_data=None` (don't resurrect a fake source / re-enable double-compress) — **regression from `3f3bbb0`**; (2) bring host-level `CoreApi` German error/warning strings into i18n; (3) extend the i18n coverage test to the 5 unscanned components.
- **Top 2 architectural improvements:** return stable **error codes + params** from `CoreApi` (UI owns translation — also fixes dynamic import errors); assert the committed/source-less invariant centrally in merge/split tests.
- **3 quick wins:** add the 5 components to `i18n.test.js`; "18 languages" → 22 in FEATURES_REQUIRED §7 + CLAUDE.md; add a merge-committed test asserting `original_data is None`.
