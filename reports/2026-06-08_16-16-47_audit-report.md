# BelegTool v3.9.3 — Release-Readiness Audit

**Date:** 2026-06-08 16:16:47
**Commit:** `ed133fe` (tag `v3.9.3`) — gate before packaging the GitHub zip + Store MSIX
**Reviewer:** Opus agent (read-only); cross-referenced `2026-06-07_22-22-30` and `2026-06-08_12-24-36`.

**Pre-flight:** pytest **414** · vitest **144** · eslint **clean** · onedir build **OK** (`dist/BelegTool/BelegTool.exe`).

## Recommendation: **GO** for packaging v3.9.3
The prior report's merge regression is **correctly and completely fixed** (`ed133fe`) and locked by tests. No new Critical/High/Medium issues. Nothing must be fixed before packaging.

## Merge-fix verification (`core/commands.py:_merge`) — CORRECT & COMPLETE
- All-committed merge hard-sets `original_data=None`, `current_data=merge(current_data)`, `is_compressed=True`, `no_compression=False` → committed invariant holds on every path; the `else` (source-fallback) branch only runs when ≥1 input still has a source.
- Differing-DPI all-committed: stays committed, keeps all pages (engine concatenates). Asserted by `test_merge_committed_nodes_keeps_all_pages_differing_dpi`.
- Mixed case: pages preserved (committed members contribute `current_data`); merged node legitimately has a source. Node(...) fields all assigned in both branches. Committed split/rotate re-reviewed OK.

## Findings

| Category | Severity | Finding | Location | Note |
|---|---|---|---|---|
| i18n | High · carry-over | Host-level `CoreApi` German error/warning strings not in i18n (lock "in use", "nichts zu exportieren/angezeigt/importiert", `ungültige Daten`, the export/import warnings, `_friendly_import_error`) → non-German UIs see German on error paths. | `core/api.py:34, 278-279, 642, 655, 673, 683, 756, 762, 784` | **Not a release blocker** — pre-existing partial-localization, error-path only, Store advertises de/en as native. Fix post-release via en.js keys or error-code+params. |
| Doc honesty | Low · NEW (fixed in follow-up) | i18n "Completeness" paragraph said "17 languages 100%" and listed only tlh as partial, omitting qya/sjn. | `CLAUDE.md:527` | Corrected to 19 complete + tlh/qya/sjn partials. |
| Packaging | Low · cosmetic | `packaging/msix_layout/AppxManifest.xml` hardcodes 3.9.1.0 but is gitignored and regenerated every build — never ships. | `packaging/msix_layout/` (untracked) | No action. |

## Verified-solid
ZIP-bomb per-member cap (`3cef204`) intact · no eval/exec/pickle/shell · MSIX version-patch regex safe (negative lookbehind protects MinVersion 10.0.17763.0) · no PII in logs · `.gitignore` guards (`*.belegtool`, `*.wsb`, msix artifacts) · i18n placeholder + 134-key locks pass · coverage scan now covers all 10 components · version bump complete (no stale 3.9.2/3.9.1 that affects the release).

## FEATURES_REQUIRED critical paths
§1–§4, §6, §7 ✓; §5 export ⚠ = the documented, consistent auto-split known gap.

## Counts
Critical 0 · High 1 (carry-over, non-blocker) · Medium 0 · Low 2 (1 doc fixed, 1 cosmetic).

## Quick wins (post-release)
1. Bring static `CoreApi` error strings into `en.js` (+ full-coverage langs) to close the carry-over High.
2. (done) CLAUDE.md completeness paragraph reconciled.
