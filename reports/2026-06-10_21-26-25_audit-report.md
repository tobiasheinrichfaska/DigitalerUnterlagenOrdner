# BelegTool — Audit (post-fix verification + fresh sweep)

**Date:** 2026-06-10 21:26:25
**Commit:** `992d763`
**Reviewer:** Fable agent (read-only); cross-referenced `2026-06-10_20-21-29` (whose findings were just implemented) and `2026-06-08_16-16-47`.
**Pre-flight:** eslint clean · vitest **164/164** · pytest (3.12) **426 passed, 90% cov** · vite build OK. Agent independently re-ran i18n/messages/selection (58/58), core_api/host (48/48), and an empirical per-language key-count check.

## Previous report (2026-06-10_20-21-29): ALL 10 findings resolved
The 8 implemented fixes were each **verified correct + regression-free**:
1. **`localizeMessage`** (`webui/src/lib/messages.js`) — all 8 templates match the exact German in `core/api.py`/`App.jsx` (drift-lock test is real); whole-templates-before-`'; '`-split ordering correct; no shadowing; 12 keys present in `en.js` **and all 18 full-coverage langs** (every map = 146 keys, 0 missing/extra); placeholder parity across 21 maps.
2. **`CoreApi.close_session`** — idempotent, no double-free with `release()`; shared-uid guard correct (closing one window keeps the other's `_pcount`/renders); `prune` deadlock-free.
3. **View-temp cleanup** — per-session binding under lock, deleted on owning close; `sweep_stale_view_dirs` only >24 h dirs; tested.
4. **`mergeableIds`** extracted to `lib/selection.js` + test (document-order, null cases).
5. **Context-menu gating** — merge + group both gated on `selectedIds.includes(node.id)`, consistent with export/status/delete; both cases tested.
6. **Accessibility** — `aria-live` error/notice; `aria-label` icon buttons; `role`/`aria-selected`/`tabIndex` on rows.
7. **`host.py`** — `logger.exception` before MotW dialog; `_is_clr_load_error` tested both ways.
8. **`_locks`/`_paths`** mutations under `self._lock`.

## Fresh findings (all Low)

| Category | Severity | Finding | Location | Suggestion |
|---|---|---|---|---|
| i18n | Low · NEW | `"Fenster nicht gefunden"` (×4) shown via `setError` but not an `en.js` key → German in non-German UIs (rare: window closed mid-call). Residual of the closed High. | `host.py:226,239,250,269` | Add key to `en.js` + full-coverage langs; key-lock 146→147. |
| i18n | Low · NEW | Drag-ghost strings hardcoded German, invisible to the t()-scan: `'📥 importieren'`, `` `in ${parentName}` ``, `'oberste Ebene'`, `` `${n} Elemente` ``. | `webui/src/Tree.jsx:170-171,201` | Route through `t()` (`'oberste Ebene'` key already exists). |
| Accessibility | Low · NEW | `aria-live` regions are mounted **together with** their text (`{error && <p aria-live…>}`) — many screen readers don't announce a region inserted with its content; only later swaps announce. | `webui/src/App.jsx:513-514` | Keep one empty `<p aria-live="polite">` always mounted; swap its text. |
| Accessibility | Low · NEW | Folder `treeitem`s lack `aria-expanded`; all rows `tabIndex={-1}` with no roving tabindex → tree not Tab-reachable. | `webui/src/Tree.jsx:104-138` | `aria-expanded={!node.collapsed}` (one-liner); roving tabindex folds into plan #10. |
| Resource lifecycle | Low · NEW (edge) | A **second running instance**'s startup sweep deletes the first instance's live `beleg_view_*` dir if open >24 h (mtime=creation); the window keeps working from memory but in-place **Speichern** then fails (parent dir gone). | `core/api.py:34-54` + `host.py:38` | Touch the dir mtime on bind in `open()`, or have save recreate the parent dir. |
| Test coverage | Low · NEW | The `close_session` shared-uid guard (same file in two windows must not evict the survivor) is code-correct but **untested** — the one scenario the prior report asked to protect. | `tests/test_core_api.py` | Open same `.belegtool` twice, close one, assert the leaf stays live. |
| i18n | Info (accepted) | 6th `_friendly_import_error` branch (`f"{name}: {msg}"`) intentionally untemplated — raw exception text. | `core/api.py:77` | None / fold into deferred #11. |
| Spec gap | Low · carry-over | Export auto-split >100 pages unwired — documented known gap. | `formats/toc_export.py` | Tracked. |

**Not re-flagged (logged as v3.10.0 plan):** #10 reusable accessible menu / context-menu keyboard nav; #11 optional error-code contract (CLAUDE.md).

## Verified solid (fresh sweep)
No `eval/exec/pickle/shell=True/os.system`; `import_bytes`/`materialize_subset` traversal-safe; `release`/`close_session`/`save(path)` not JS-exposed (host-native, dialog-gated); logic/UI split held (new pure logic in `lib/` with tests); docs match code (146 keys, 19 full langs, close_session/sweep/messages documented); FEATURES §1–§4/§6/§7 ✓ (§5 = tracked gap); logging opt-in, no PII; deps current-gen, validated by the 426-test run.

## Summary
- **Counts:** Critical 0 · High 0 · Medium 0 · Low 7 (6 NEW polish/edge + 1 carry-over) · 1 info.
- **Previous report fully resolved**; no regressions from the fixes.
- **Top 3 fixes:** (1) `"Fenster nicht gefunden"` → en.js + langs (lock→147); (2) key the Tree.jsx drag-ghost strings; (3) keep `aria-live` regions permanently mounted.
- **3 quick wins:** shared-uid `close_session` regression test; `aria-expanded` on folder rows; touch view-dir mtime on `open()`.
