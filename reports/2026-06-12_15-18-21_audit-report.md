# BelegTool — Audit (focused dual-pass re-audit + fix-verification)

**Date:** 2026-06-12 15-18-21
**Commit:** `2121a95` (the commit that resolved the 2026-06-11 round)
**Method:** two focused **Fable** agents — a security re-audit and a concurrency/correctness re-audit, each tasked to **both verify last round's fixes and hunt fresh** — synthesized by Opus. Pre-flight (HEAD): eslint clean · vitest 188 · pytest 454 (91%) · build OK.

**Headline:** the verification caught that **last round's High fix is incomplete** — the variant-blob bomb cap was applied one layer too high, so a hostile `.belegtool` can still OOM-crash on open via a different decompression layer. Both agents found this independently. All 14 other fixes (incl. the new locking) are **confirmed correct, with no deadlock or regression introduced.**

## Findings

| Category | Severity | Finding | Location | Suggestion |
|---|---|---|---|---|
| Security / DoS | **High · fix INCOMPLETE** | **Variant-blob bomb still reachable on open — one layer up.** Last round capped the *inner* stored-ZIP in `variant_blobs.unpack` (correct). But `seed_variants_from_file` first calls `pdf.attachments[name].get_file().read_bytes()` for **every** `variant_*` attachment **before** `unpack` runs — and pikepdf transparently inflates that embedded-file's `/FlateDecode` stream with **no cap**. A FlateDecode-bombed `variant_*` attachment (~10 MB raw → ~10 GB, deflate ≈1030:1; up to 500 attachments accumulate in `data_by_id`) → OOM on opening a malicious `.belegtool`. Same automatic-on-`open()` attack class as the original High; the inner cap sits below where the bomb detonates. (Legit blobs are `ZIP_STORED` PDFs → raw≈decompressed, so a tight raw-size cap is free.) | `services/variant_store.py:120` (`read_bytes()`), then `:132`→`variant_blobs.unpack` | Before `read_bytes()`, check the attachment's **raw/compressed** stream size (`read_raw_bytes()`) and skip over a small budget; ideally inflate incrementally (`zlib.decompressobj` + `max_length`) with a running-total cap, mirroring `unpack`. |
| Security | **Medium** | **OOXML `.rels` scan fails OPEN on a parse error.** `scan_ooxml_external_targets`' outer `except Exception: return None` → any scan failure is treated as clean and `office_via_com` opens the file anyway. A `.docx` that `is_zipfile` accepts but whose `.rels` Python's `zipfile` can't read (unsupported method / corrupt local header) while Word's lenient OPC parser still resolves the external attached-template = bypass → NTLM-hash leak / SSRF on open. Also misses `ftp:`/`ftps:` schemes. | `universal_importer/converters.py:140-164` (esp. `:162`), refusal `:177-181` | For a file that **is** a zip, fail **closed** on a scan exception (refuse / log+refuse). Add `ftp:`/`ftps:` to `_RELS_EXTERNAL_PREFIXES`. |
| Lifecycle | **Low** | **Cancel-token revive misses two edges** (cousins of the fixed Medium; both self-heal on the next successful edit). **N2:** tokens are `set()` *before* the mutate but revived only on success → a command that **fails** (e.g. `DeleteMany` "Gemischte Auswahl", a Merge validation error) leaves still-present nodes' tokens set → their `compress_options` return `[]` until the next successful dispatch/undo/redo. **N3:** `open()` and `import_paths` don't call the revive hook → delete-without-save in window A then open the same file in window B (uid persists) leaves that node uncompressable in B until B's first edit. | `core/api.py:405-418` (N2), `:311-359, 883-910` (N3) | Set tokens *after* a successful `_mutate` (or also revive on `ok:False`); call `_revive_cancel_tokens(sid)` at the end of `open()`/`import_paths`. + tests. |
| DoS | Low | **Compression render path caps width only, not pixel budget.** `MAX_RENDER_PIXELS` was added to `render.py` but not to `compress_pdf_bytes._render_one_page` (caps `max_width_pt=595`, height unbounded). Reached automatically on open via the proactive compress sweep of cheap leaves. For spec pages the width-clamp keeps the worst case (~37 MP) under the 64 MP cap, so impact is bounded — a defense-in-depth inconsistency, not a clean OOM. | `formats/compress_pdf_bytes.py:144-154` | Apply the same pixel-budget clamp in `_render_one_page`. |
| DoS | Low | `.rels` scan enumerates `namelist()` of an untrusted zip with no entry cap (Office import path doesn't go through `archives.py` guards). Minor memory DoS on a millions-of-entries `.docx`. | `converters.py:147-148` | Cap entry count before iterating. |
| Memory | Low | **`_dcache` retention.** The identity-memoised digest cache holds strong refs to up to **256 full leaf byte-strings** (the strong ref is what makes id-keying safe — correctness is fine), including bytes of *closed* sessions and pre-compress/rotate versions → potentially hundreds of MB pinned until evicted. | `core/engine.py:66, 79-81` | Evict by byte-budget, or clear engine `_dcache` in `close_session` alongside `_pcount`. |
| Error handling | Low | `host._bind_close` still wraps `release`/`close_session` in `except Exception: pass`. The lock fix removed the known RuntimeError trigger, but any *other* close failure still silently leaks the whole session with no trace. | `host.py:98-104` | `logger.exception(...)` instead of `pass`. |
| Docs / dead code | Low | The `2121a95` commit message says "remove dead any_dirty/render/renderCompressed", but only the **JS wrappers** were removed; `CoreApi.render`/`render_compressed` (api.py:460-512) + their `HostApi` bridge exposures (host.py:191-195) remain with **no UI caller** — dead product surface on the JS bridge (harmless, read-only) and the api.py module docstring still advertises them. | `core/api.py:460-512`; `host.py:191-195` | Remove or annotate as IPC/test-only; fix the docstring. |

**Info (benign):** `_last_seed` unlocked write (atomic tuple swap); `_view_touched` entries not pruned on close (one float per view dir); a `_count_for` insert racing `close_session` can re-add a just-pruned `_pcount` key (one int); `render_compressed_window`/`page_count` don't touch the view dir (any visible page goes through `render_window`, so fine).

## Confirmed correct (14 — both agents, nothing broken)
- **New locking introduced NO deadlock:** every `_count_for` caller traced — all run lock-free (callers release `self._lock` before calling); lock ordering `CoreApi._lock → engine _mcache_lock/_dcache_lock → RenderService locks` is **acyclic** (engine/render never call back into CoreApi); `self._lock` is a non-reentrant `Lock` and no newly-locked region re-acquires it.
- `_persisted` fully guarded by `_mcache_lock`; no expensive op under any lock.
- Cancel-token revive + live-elsewhere guard correct for the two reproduced scenarios (an in-flight cancelled compress is **not** memoised → no poisoned cache).
- **Identity-memoised `_digest` has no stale-digest risk:** the cache entry holds a strong ref to the bytes, so an entry's `id()` can't be reused while live; after eviction it simply recomputes. Distinct objects with equal content → equal sha1 → consumers unaffected.
- undo/redo prune+prewarm hook; periodic view-dir touch; `rangeIds` (visible-rows-only, `flattenIds` fully removed); `run().catch` (all downstream consumers null-safe); folder-preview skip (behaviourally identical); `Reset` `_require_leaf` (tighter than before); double-checked lazy init ×3 (safe in CPython); variant inner-ZIP caps; signature gate on the path branch; `.rels` matching logic (case/`HTTP://`/UNC/protocol-relative covered, hyperlinks exempt, all `.rels` scanned); Office `Quit()`-in-`finally` per app; pixmap DPI clamp on both `render.py` paths; bridge docstring; i18n 151-key lock (`en.js` = 151, verified by execution); refusal message localizes end-to-end.
- Security still-solid: no eval/pickle/exec/shell; no Zip-Slip; `materialize_subset`/`import_bytes` path-traversal clean (name sanitized into fresh `mkdtemp`); JS-bridge write destinations come from native dialogs/temp dirs; logging the `.rels` target logs the attacker's own callback host, not victim PII.

## Summary
- **Counts:** Critical 0 · **High 1 (incomplete fix)** · **Medium 1** · **Low 6** · Info ~4.
- **Top 3:** (1) **N1 — variant attachment FlateDecode bomb** is the one that matters: last round's High fix is one layer too high, so the crash-on-open-from-a-hostile-`.belegtool` vector is still open. Cheap raw-size cap closes it. (2) **`.rels` scan fails open** on a parse error → Office-template SSRF/NTLM bypass; fail closed for zips + add `ftp:`. (3) finish the cancel-token lifecycle (N2/N3) — narrow, self-healing.
- **Release call:** **do not cut v3.9.4 until N1 (the High) is closed** — it re-opens the exact crash-on-open the last release aimed to fix. The Medium (`.rels` fail-open) is cheap and worth doing in the same pass. Lows are backlog. Net: last round's work is solid (14/14 fixes correct, no deadlock); this round is essentially "finish the bomb fix at the right layer."
