# Audit Report — DigitalerUnterlagenOrdner (BelegTool) v3.9.3

**Date:** 2026-06-13 07:41
**Method:** Single-agent (Opus 4.8) read-only audit under the improved `/audit` grounding rules
(every finding opened in source + verified; exact `file:line`; Confirmed vs Needs-verification).
**Verdict:** Mature, heavily-hardened codebase (13th audit pass). **0 Critical, 0 High.**
The prior pass's open items (H-1 Ctrl+wheel, H-2/H-3 archive test gaps) are **fixed in the
current — uncommitted — working tree** and verified green. New findings are a cluster around an
**office-detection subsystem that was built but never wired in**, plus a residual group-by-tag
selection-count inflation. All Medium/Low; no data-loss or security-critical issue found.

---

## Pre-flight checks (all green)

| Check | Result |
|---|---|
| `pytest` (backend, `.build_venv`) | ✅ **486 passed** in 46.6 s — **91 %** coverage |
| `vitest` (frontend) | ✅ **213 passed** (21 files) |
| `eslint .` (frontend) | ✅ **0 errors** |
| `vite build` (prod) | ✅ built in 0.39 s — `index.js` 411.42 kB (gzip 114.97 kB) |
| FEATURES_REQUIRED.md | ✅ present, 7 critical paths; all map to real code (caveat: path 1 — see M-1) |
| Convention files | ✅ `manual_tests/` (01–08 + README), `docs/data-model.html`, `.gitattributes`, CLAUDE.md back-link all present |
| Repo hygiene | ✅ `packaging/msix_layout/`, `build/`, `dist/`, `.coverage`, `.build_venv/` all correctly gitignored (only manifest + scripts tracked) |
| git working tree | ⚠️ **dirty** — 7 modified + 3 new files; the modifications are sound fixes (see below), not yet committed |

### Prior-pass items — re-verified
- **H-1 (Ctrl+wheel zoom dead):** ✅ FIXED — `App.jsx:72-89` now binds via a **callback ref**
  (`previewRef = useCallback`), so the wheel listener attaches when the preview element actually
  mounts (not during the loading screen). New `App.wheelzoom.test.jsx` covers it; suite green.
- **H-2 / H-3 (archive bomb actual-read + invalid-archive untested):** ✅ FIXED — new
  `tests/test_archive_errors.py` present; backend suite 480→486.
- **Uncommitted fixes also verified sound:** `core/api.py:723-740` (abort save if the existing
  non-empty file can't be backed up — prevents an unrecoverable truncated overwrite);
  `compress_pdf_bytes.py:59-70` (fall back to original if compression dropped a page);
  `PreviewControls.jsx` (only clear the undecided dot / bake no-gain on a **successful** eval);
  `converters.py` ODF routing.

---

## Findings

| # | Category | Sev | Conf. | Finding | Location | Suggestion |
|---|---|---|---|---|---|---|
| **M-1** | Features / Import | **Medium** | Confirmed | **Office & ODF files nested in a ZIP/TAR/e-mail never import** — they show as "… – nicht importierbar". Root cause: `UniversalImporter.initialize()`/`_detect_office_support()` is **never called** (grep: zero callers in app or tests), so `_has_word/_excel/_powerpoint` stay `False` forever → `get_supported_extensions()` omits `.doc(x)/.xls(x)/.ppt(x)/.odt/.ods/.odp`. Archive/e-mail members go through `UniversalImporter.convert(bytes, name=…)` (the **bytes** branch), which gates on `get_supported_extensions()` and raises *"Nicht unterstützter Dateityp: .docx"*. (Top-level Office import via dialog/drag still works — it takes the path branch, which routes by the static `OFFICE_EXTENSIONS` and skips that gate.) **Empirically reproduced.** Domain-relevant: e-mails with Office attachments are common for receipts/tax docs. | `universal_importer/importer.py:91-96,162` (gate) · `archives.py:86,142,177` (bytes-branch callers) · root cause: `importer.py:233-235` never called | Call `UniversalImporter.initialize_async()` once at host startup (e.g. in `host.main()` after `CoreApi()` or in `_prewarm`), **or** make the bytes branch route Office/ODF by the static `OFFICE_EXTENSIONS` like the path branch does. Add a regression test importing a ZIP/EML that contains a `.docx`. |
| **M-2** | React / Silent failure | **Medium** | Confirmed | **Group-by-tag shift-range selection inflates the selection count.** In a group-by-tag view the range runs over the *grouped* `treeForView`, whose `visibleOrder` includes synthetic `__tag__*` group rows **and** the same real leaf duplicated under multiple tags. `rangeIds` returns all of them (`treeNav.js:46-53`), so `selectedIds` carries synthetic + duplicate ids. The operations themselves are safe (export's set-walk `api.py:820-832` simply never matches a synthetic id; `SetStatusMany` skips ids not in the doc `commands.py:338-340`; merge/group/delete are disabled in views) — **but the counts shown to the user are wrong**: Toolbar "Export PDF (Auswahl N)" and ContextMenu "Status (N)" count synthetic/duplicate ids while fewer real nodes are processed. *(Note: the prior report's claim that export emits a raw English `node not found` is inaccurate — export uses set-membership, not the dispatch error path.)* | `webui/src/App.jsx:314-325` (range) · `webui/src/lib/treeNav.js:30-53` · count labels: `Toolbar.jsx:22`, `ContextMenu.jsx:137,150` | In `select()`, filter the range to **real, unique** ids before `setSelectedIds`: `range.filter(id => !isGroupNode(id))` then de-dupe. Optionally also resolve `SetStatusMany` ids through `resolveSel` like export/delete do. Add a tags-view range-selection test. |
| **L-1** | Dead code / Maintainability | **Low** | Confirmed | **An entire office-detection subsystem is unused** (root cause of M-1, called out separately so the fix removes/​wires it deliberately): `initialize`, `initialize_async`, `_detect_office_support`, `is_supported`, `get_filetypes_for_dialog`, and the `_has_word/_excel/_powerpoint` flags have **no callers** outside the class. | `universal_importer/importer.py:80-128,217-251` | Either wire `initialize_async()` in at startup (fixes M-1 and makes `get_filetypes_for_dialog`/`is_supported` meaningful) or delete the dead surface. Don't leave it half-connected. |
| **L-2** | Consistency / UX | **Low** | Confirmed | **The native import dialog's "supported files" filter is a hardcoded subset** that omits real, supported types — notably the **just-added `.odt/.ods/.odp`**, plus `.doc/.xls/.ppt`, `.bmp/.tif/.tiff`, `.txt/.rtf/.md`, `.html`, `.tgz`, `.dbeleg`. The purpose-built dynamic filter `UniversalImporter.get_filetypes_for_dialog()` (which lists all of these) exists but is never used; `host.py` hand-rolls its own tuple. Effect: a user must switch to "Alle Dateien" to import a freshly-supported `.odt`. | `host.py:74-78` (`IMPORT_FILE_TYPES`) vs unused `importer.py:99-120` | Have `import_dialog` build `file_types` from `UniversalImporter.get_filetypes_for_dialog()` (after L-1's `initialize` so Office/ODF appear), instead of the static tuple. |
| **L-3** | Security / DoS (defense-in-depth) | **Low** | Confirmed | **E-mail attachment bomb-guard is weaker than the zip/tar one.** ZIP/TAR cap *actual* bytes by reading `remaining+1` and aborting (`archives.py:78-83,135-140`). The e-mail path instead fully materializes each part via `part.get_payload(decode=True)` / `att.data` and only checks the running total **at the top of the next iteration** — so a single oversized attachment is decoded into memory in full before any cap fires. Bounded overall by the user choosing to import the file, hence Low. | `universal_importer/archives.py:262-266,271-275,313-317` (vs the streaming cap at `78-83`) | Before accepting an attachment, check `decoded_total + len(content) > _ARCHIVE_MAX_UNCOMPRESSED_BYTES` and emit `_not_importable(..., "zu groß")` instead of appending; mirror the zip/tar early-abort. |

---

## Summary

**Totals:** 0 Critical · 0 High · 2 Medium · 3 Low.

**Top fixes (priority order):**
1. **M-1 / L-1** — wire `UniversalImporter.initialize_async()` in at host startup (one line). Fixes
   the office-in-archive import gap **and** activates the dead `get_supported_extensions`/dialog
   filter at once. Add the regression test.
2. **M-2** — filter synthetic/duplicate ids out of the group-by-tag range selection so the shown
   counts match what is actually processed.
3. **L-2** — switch the import dialog to the dynamic `get_filetypes_for_dialog()` (depends on #1)
   so the new ODF support is discoverable.

**Architectural notes worth considering:**
- The office-detection feature (L-1) is a clean example of *build-then-forget*: the logic layer is
  correct and tested in isolation, but no caller connects it to the running host. A small
  integration test that drives `host.main()`'s startup wiring (or asserts `initialize` is called)
  would have caught M-1/L-1 — the unit suite can't, because it sets `_has_*` implicitly.
- Consider the long-deferred **error-code contract** (CLAUDE.md Open item #11): M-2's analysis shows
  the frontend still reasons about backend German strings; codes would remove that coupling.

**Already-tracked (not re-flagged):** manual tests 01–04 describe legacy Tk flows (Known Limitation);
the >100-page export auto-split not wired into the UI (FEATURES_REQUIRED §5); the accepted
audit-info residuals listed in CLAUDE.md (2026-06-12).

**Pre-flight:** build ✅ · lint ✅ · backend tests ✅ 486 · frontend tests ✅ 213.
