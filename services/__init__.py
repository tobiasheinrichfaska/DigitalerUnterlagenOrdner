"""Headless processing services (no Tk / no GUI).

Stateless, bytes-in/bytes-out operations that the GUI and a future React+backend
UI both build on. Part of the core/GUI decoupling (see
docs/REACT_MIGRATION_PLAN.md, Phase 1).
"""
